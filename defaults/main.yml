---
known_hosts_path: "{{ ansible_user_dir }}/.ssh/known_hosts"
#known_hosts_public_keys:
#  - name:
#    key:
# Keys have to be manually prepared to conform to the following format:
# <name1>,<name2>,<name3> <keytype> <key>

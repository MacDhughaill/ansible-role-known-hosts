---
# ssh-keyscan has an option (-t) to specify which type of encryption algorithm you wish to scan for keys of.
# The following variable allows you to choose which type. Consult the ssh-keyscan man page for the full selection of types.
known_hosts_key_encrypt_algorithm: ecdsa
# Enter here a pattern for selecting hosts as described in Ansible's documentation.
# https://docs.ansible.com/ansible/latest/user_guide/intro_patterns.html
known_hosts_selection_pattern:
